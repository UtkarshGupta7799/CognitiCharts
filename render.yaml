services:
  - type: web
    name: cogniticharts
    env: docker
    plan: free
    region: oregon 
    branch: main
    repo: https://github.com/YOUR_USERNAME/CognitiCharts # Placeholder, user needs to update this or connect manual repo
    healthCheckPath: /_stcore/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
